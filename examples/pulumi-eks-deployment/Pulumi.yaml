name: create-eks
runtime: python
description: A simple EKS cluster deployment using Pulumi
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: ap-southeast-1
name: pulumi-sqs-deployment
runtime: python
description: AWS SQS Queues deployment with various messaging configurations
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
name: pulumi-dynamodb-deployment
runtime: python
description: AWS DynamoDB Tables deployment with various configurations
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
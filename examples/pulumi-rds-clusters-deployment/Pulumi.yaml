name: pulumi-rds-clusters-deployment
runtime: python
description: AWS RDS Aurora Clusters deployment with MySQL and PostgreSQL
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
name: pulumi-rds-instances-deployment
runtime: python
description: AWS RDS Instances deployment with MySQL and PostgreSQL databases
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1